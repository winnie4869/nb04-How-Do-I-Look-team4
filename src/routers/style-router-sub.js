// import express from 'express';
// import * as styleController from '../controllers/styleController.js';

// const router = express.Router();

// // 스타일 관련 라우트 정의
// router.get('/', styleController.getStyles); // 스타일 목록 조회 (갤러리)
// router.post('/', styleController.createStyle); // 스타일 등록
// router.get('/ranking', styleController.getRanking); // 스타일 랭킹 목록 조회
// router.get('/:styleId', styleController.getStyleDetail); // 스타일 상세 조회
// router.put('/:styleId', styleController.updateStyle); // 스타일 수정
// router.delete('/:styleId', styleController.deleteStyle); // 스타일 삭제

// export default router;